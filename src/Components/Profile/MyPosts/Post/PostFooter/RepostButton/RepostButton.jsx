import s from './RepostButton.module.css';

const RepostButton = () => {
    return (
        <div className={s.repostButton}>
            repostButton
        </div>
    );
}

export default RepostButton;