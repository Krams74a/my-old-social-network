import s from './RepostButton.module.css';

const RepostButton = () => {
    return (
        <button className={s.repostButton}>
            Репост
        </button>
    );
}

export default RepostButton;