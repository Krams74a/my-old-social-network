import s from './CommentButton.module.css';

const CommentButton = () => {
    return (
        <div className={s.commentButton}>
            CommentButton
        </div>
    );
}

export default CommentButton;