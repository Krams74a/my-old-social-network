import s from './CommentButton.module.css';

const CommentButton = () => {
    return (
        <button className={s.commentButton}>
            Комментарий
        </button>
    );
}

export default CommentButton;